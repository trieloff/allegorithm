{
  "name": "allegorithm",
  "version": "0.1.0",
  "type": "module",
  "description": "A language where programs are interpreted, not just executed",
  "main": "dist/index.js",
  "bin": {
    "allegorithm": "./src/cli.ts",
    "nacre": "./src/nacre/cli.ts"
  },
  "scripts": {
    "build": "tsc",
    "build:as": "npx tsx src/nacre/cli.ts src/nacre/prelude.nacre src/nacre/as.nacre",
    "build:expand": "npx tsx src/nacre/cli.ts src/nacre/prelude.nacre src/nacre/expand.nacre",
    "build:expand-gc": "npx tsx src/nacre/cli.ts src/nacre/prelude.nacre src/nacre/expand-gc.nacre",
    "build:native": "clang --target=wasm32 -O2 -nostdlib -Wl,--no-entry -Wl,--export=poke -Wl,--export=crc32 examples/native/crc32.c -o examples/native/crc32.wasm && rustc --target wasm32-unknown-unknown --crate-type=cdylib -C opt-level=z -C lto -C strip=symbols -C panic=abort examples/native/fmix.rs -o examples/native/fmix.wasm",
    "build:web": "npx tsx src/nacre/build-web.ts",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@types/node": "^22.19.11",
    "binaryen": "^132.0.0",
    "playwright-core": "^1.62.1",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  },
  "license": "Apache-2.0"
}
